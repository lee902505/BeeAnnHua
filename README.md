# 星辰日记 V0.13.30

## V0.13.30 — 农场动态中心

- 好友页改为三个分页：`动态 / 好友 / 申请`，不再把所有内容堆在同一个长页面里。
- 新增统一「农场动态」：好友拜访与偷菜都会留下时间纪录，并可直接点击「回访」。
- 好友拜访改用 `get_friend_farm_v3`；真正进入好友农场时记录一次拜访，10 分钟内重复刷新不会洗版。
- 偷菜继续使用服务器端固定 ×1 规则；新的偷菜会自动写入统一动态，不影响原本 `farm_steals` 的防重复偷菜纪录。
- 旧的偷菜历史会在 migration 执行时回填到动态中心，既有纪录不会消失。
- 动态中心最多保留每位农场主最近 100 笔显示纪录；页面一次读取最近 50 笔，避免 Supabase 资料无限膨胀。
- 好友按钮红点现在统一计算：`待确认好友申请 + 未读农场动态`。
- 打开动态页后会把目前动态标记已读；每笔刚读到的新纪录仍保留 `NEW` 标示，方便辨认刚发生的事件。
- 动态时间改为较自然的「刚刚 / X 分钟前 / X 小时前 / 昨天 HH:mm」。
- 已把下一阶段使用的 NPC / 虫害 / 装饰 4×4 Sprite Sheet 收进 `images/farm/farm-event-sheet-01.webp`，本版尚未启用，预留给后续天气与虫害系统。

### 必须执行的新 SQL

在 Supabase SQL Editor 执行：

`supabase/migrations/20260926_014_farm_activity_center.sql`

该 migration 会：

- 建立 `farm_activity` 统一动态表。
- 把现有 `farm_steals` 历史回填到动态中心。
- 新增偷菜纪录自动写入动态的 trigger。
- 新增带拜访纪录的 `get_friend_farm_v3()`。
- 新增 `get_farm_activity_v1()` 读取／标记动态。
- 不会清空等级、金币、农田、作物、好友、任务、称号或既有偷菜资料。

执行成功显示 `Success. No rows returned` 后，再上传 V0.13.30。

---

## V0.13.28 — 农田照料 UI 微调

- 取消农田植株旁边的已浇水／已施肥小状态球，让农田画面更干净。
- 浇水与肥料状态仍保留在单格作物详情，不影响照料逻辑和存档。
- 肥料商店改为和种子商店一致：显示「价格 / 包」，下方按钮为「买 1」「买 5」。
- 肥料价格、效果、Supabase 存档逻辑完全不变。
- 已执行 V0.13.27 的 013 migration 后，本版不需要新增 SQL。

---

## V0.13.28 — 农田照料系统 + 正式工具 Sprite Sheet

- 新增第三张 4×4 Sprite Sheet：耙子、浇水壶、蔬果盲盒、三级肥料、照料状态与特效统一使用一张透明大图读取指定格。
- 农田上方新增「一键浇水」，与「一键收获」并列；每株普通作物每轮只能免费浇水一次。
- 浇水会让该轮成长时间乘以 0.92（约缩短 8%），已浇水或已成熟作物不会重复处理。
- 商店新增低／中／高级肥料：12 / 28 / 55 金币；每株每轮最多使用一包，分别缩短 10% / 20% / 30% 成长时间。
- 浇水与肥料采用乘法叠加，例如高级肥料 + 浇水为 `原时间 × 0.70 × 0.92`。
- 蔬果盲盒维持原规则：固定 4 小时，不受浇水或肥料加速影响；成长中的盲盒改用正式盲盒素材。
- 点击成长中的普通农田可查看照料状态，并单格浇水／施肥；农田角落会显示已浇水与肥料状态小图标。
- 一键收获的旧 SVG 图示改为正式耙子素材；新增浇水与施肥的短暂 Flash 风格特效。
- 好友拜访页会读取浇水／肥料后的真实成熟时间；服务器偷菜成熟判定同步支持照料加速。

### 必须执行的新 SQL

在 Supabase SQL Editor 执行：

`supabase/migrations/20260926_013_farm_care_tools.sql`

该 migration 只更新偷菜的服务器端成熟判定，让它与浇水／肥料后的成长时间一致；不会清空农场、金币、等级、好友、作物或历史纪录。

执行成功显示 `Success. No rows returned` 后，再上传 V0.13.28。

---

## V0.13.26 — 升级演出 + 土地解锁动画 + 每日任务 + 红点导航

- 新增完整 `LEVEL UP!` 演出：升级时显示 Lv. 前后变化、解锁农地数量与新作物。
- 达到 Lv.3 / 5 / 8 / 12 / 16 / 20 / 25 时，新开放农地会播放锁头晃动、消失与土地亮起动画。
- 任务页升级为四分页：`每日 / 新手 / 成就 / 称号`。
- 每日任务以 **UTC+8 00:00** 为重置基准，并透过 Supabase 服务器日期校正：
  - 今日播种 5 格 → 金币 ×15
  - 今日收成 5 格 → EXP +20
  - 今日出售 10 个作物 → 金币 ×20
  - 今日拜访 2 位不同好友 → EXP +15
  - 今日成功偷菜 1 次 → 金币 ×10
  - 全部完成可领取全勤奖励：蔬果盲盒 ×1 + EXP +30
- 新增分层红点导航：主画面「任务」按钮、任务分页、成就分类都会显示可领取数量。
- 新称号解锁后「称号」分页会出现红点，打开称号页后清除。
- 好友按钮新增红点：未处理好友申请与未读偷菜纪录会显示数量。
- 继续保留 V0.13.25 的第二批 Sprite Sheet、固定 ×1 偷菜与偷菜纪录。

### 必须执行的新 SQL

在 Supabase SQL Editor 执行：

`supabase/migrations/20260926_012_farm_daily_tasks.sql`

该 migration：
- 新增 `get_farm_day_v1()`，用服务器端 UTC+8 日期校正每日任务重置。
- 新增 `steal_friend_crop_v4()`，成功偷菜时由服务器同步累积「今日偷菜」次数。
- 不会清空农场、金币、等级、好友、作物或历史偷菜资料。

执行成功显示 `Success. No rows returned` 后，再上传 V0.13.26。

---

## V0.13.24 — 作物定位与文字层级修正

- 第一批 Sprite 作物整体沿农田透视方向向右上微调（桌机约 +8px / -7px；手机更克制）。
- 作物名称与倒数时间跟随作物一起微调。
- 有作物的农田层级提高到裸地／锁定农田之上，避免名称与倒数被相邻农田盖住。
- Hover / 点击时维持最高交互层级，农田命中区域与既有密合排列不变。
- 无 Supabase schema 变更，不需要执行新的 SQL。


## V0.13.24 — 作物整体位置再次上移

- 保留 V0.13.18 的无土 4×4 Atlas、固定 Anchor、各作物 scale 与成熟动画。
- 红萝卜、小麦、玉米、番茄的 Sprite 相对 V0.13.18 再整体上移约 8px，让根部更贴近斜向农田的视觉种植线。
- 不移动倒数标签、作物名称、农田命中区或农田本身。
- 无数据库结构变更，不需要重新执行 Supabase SQL。


## V0.13.24 — 作物位置微调

- 根据实机画面，将第一批 Sprite Sheet 作物整体相对 V0.13.17 **向上移动 10px**。
- 红萝卜、小麦、玉米、番茄的大小、Atlas 取格、根部 Anchor 与成熟动画保持不变。
- 不涉及 Supabase schema / RPC 变更，无需执行新的 SQL。

## V0.13.24 — 作物 Sprite Sheet 成长素材（第一批）

- 红萝卜、小麦、玉米、番茄改用同一张 4×4 Sprite Sheet，不拆成 16 个文件。
- 每种作物依真实成长进度在 30% / 60% / 90% 切换四个视觉阶段。
- 90% 后会看到完整成熟外观，但必须到 100% 才能收成。
- 保留原本倒数、成熟跳动、密合农田、好友拜访与偷菜逻辑。
- 草莓、南瓜、葡萄、星辰果暂时维持 Emoji，等第二张 Sprite Sheet 完成再接入。
- 本版没有新的 Supabase migration，不需要执行 SQL。



## V0.13.14 — 长期成就与称号系统

- 新玩家默认拥有并装备称号 **【新手】**。
- 任务页新增「新手任务 / 成就 / 称号」三个分页。
- 长期成就包含财富、收成、播种、盲盒、偷菜、成长、好友七条路线。
- 金币成就使用「历史最高持有金币」判定，达到后即使后来消费也不会倒退。
- 收成里程碑支持 1 / 5 / 10 / 25 / 50 / 100 / 500 / 1000 格。
- 可解锁并自由装备称号，例如【新手农夫】【农夫】【万元户】【农场富翁】【传奇农夫】【偷菜高手】【星辰农场主】。
- 当前装备称号会显示在自己的农场标题、排行榜、好友列表与好友拜访页。
- 蔬果盲盒种植次数与成功偷菜次数正式纳入长期统计。

### 必须执行的新 SQL

在 Supabase SQL Editor 执行：

`supabase/migrations/20260924_010_farm_achievements_titles.sql`

执行成功后再上传 V0.13.14。该 SQL 不会清空现有农场，只新增带称号字段的多人 RPC，并让新版偷菜 RPC 同步累计偷菜次数。

---

## V0.13.13 — Supabase 流量优化

这版保持 V0.13.12 的农场玩法、Revision Guard、排行榜、好友、拜访与偷菜不变，只优化云端同步的流量与请求方式。

- 农场停留期间不再每 20 秒下载整份 `farm_saves.state`。
- 改为每 60 秒只读取 `revision`；只有 revision 真的变化时，才下载完整农场 JSON。
- App / 浏览器切回前景时，若距离上次检查超过 15 秒，会先做一次轻量 revision 检查。
- 自己种植、收成、商店、任务等仍会即时写入云端，不会等待 60 秒。
- 新增 `save_farm_state_v3`：正常存档成功只回传 revision / 时间等少量 metadata，不再把完整农场 state 原样传回浏览器。
- 只有发生多分页 / 多设备 revision 冲突时，服务器才回传完整权威 state 供客户端合并。
- 旧 `save_farm_state_v2` 的浏览器执行权限会关闭，避免旧分页继续产生不必要流量。

### 必须执行的新 SQL

已执行过 008 的项目，请再执行：

`supabase/migrations/20260924_009_farm_traffic_optimization.sql`

Supabase SQL Editor → New Query → 贴上整份 → Ctrl+A → Run。
成功应显示 `Success. No rows returned`。

执行 009 后再上传 V0.13.13。

---

## V0.13.12 修正

- 修正批量种植视窗里「预计成熟」容易被误读成倒计时的问题。
- 现在红萝卜会明确显示 `20 分钟后`，并在下方补充 `约 HH:mm 成熟`；其他作物与 4 小时盲盒同样使用一致格式。
- 农田实际倒计时逻辑不变，红萝卜仍从种下时开始完整倒数 20:00。
- 不需要新增或重跑 Supabase SQL。


## 农场云端存档：Revision Guard 根治版

这版针对已经实机确认的回滚问题：Supabase 曾先写入 `120 / ["welcome"]`，随后同一 `user_id` 又被较晚的客户端写回 `100 / []`。这不是数据库自动回滚，而是第二次客户端写入覆盖了正确状态。

本版改动：

- 新增 `farm_saves.revision` 服务器版本号。
- 所有浏览器存档改用 `save_farm_state_v2`，必须携带当前服务器 revision。
- revision 不匹配时拒绝旧快照覆盖，并返回服务器权威状态。
- 关闭 `authenticated` 对 `farm_saves` 的直接 `insert/update/delete` 权限，旧版/缓存页面不能再直接覆盖农场。
- 停用旧 `save_farm_state(jsonb,bigint)` 的浏览器执行权限。
- 朋友偷菜等服务器端修改也会自动推进 revision。
- 排行榜/好友面板不再为了读取资料强制写一次完整农场存档。
- 好友数量只做本机 UI 快取，不再触发完整农场写入。

### 必须执行的新 SQL

在 Supabase SQL Editor 执行：

`supabase/migrations/20260924_008_farm_revision_guard.sql`

执行成功后再上传 V0.13.12。

---

## V0.13.7 cache refresh fix

- Farm page assets now use `?v=0.13.7` so browsers do not reuse V0.13.5 JavaScript after deployment.
- Homepage farm link uses `pages/farm.html?v=0.13.7` to force a fresh farm HTML load.
- Keeps the V0.13.6 F5/cloud-sync race fix.

# V0.13.6 — F5 存档一致性修复

本版修复两个会在重新整理页面后出现的存档问题：

- 任务奖励领取后立刻 F5，任务可能再次变成可领取。
- 蔬果盲盒种下后立刻 F5，农田上的盲盒可能被旧云端存档覆盖而消失。

## 修正方式

- 新增本机「未同步变更」标记。任何农场操作写入 localStorage 时，会先记录 dirty 状态。
- F5 / 重开页面后，只要检测到有未完成云端同步的本机变更，就优先保留本机状态并补推到 Supabase，不再单纯依赖浏览器时间与服务器时间比较。
- 任务领奖与批量播种结束后，会主动尝试立即同步 Supabase。
- 页面离开时增加 best-effort 云端同步；即使浏览器来不及完成，dirty 标记仍会在下一次加载时保护本机状态。
- 好友偷菜 RPC 返回的状态属于服务器权威状态，会正确标记为已同步。

## Supabase

V0.13.6 不需要执行新的 SQL。既有 003～006 migration 保持不变。

## 建议测试

1. 领取「欢迎来到星辰农场」奖励后立刻按 F5，确认仍显示「已领取」，金币不会再次增加。
2. 购买并种下蔬果盲盒后立刻按 F5，确认盲盒仍留在原农地且倒数继续。
3. 普通作物、商店购买、背包出售后也可快速 F5，确认状态不会回退。
4. 等右上角显示「☁ 云端已同步」后再次重整，确认云端与本机状态一致。


## V0.13.6 — F5 云端同步竞态修复

- 修正任务奖励领取后快速 F5 可能重新出现「领取奖励」并可重复拿金币。
- 修正蔬果盲盒 / 普通作物种下后快速 F5 可能被旧云端存档覆盖。
- 云端上传改为冻结快照 + 严格单调本机 revision。
- 上传期间若又发生新操作，不再把新版误标为已同步；会自动排队补传最新状态。
- 上传失败时保留 dirty 标记，下一次载入优先保护本机新状态。
- 本版不需要新增或重跑 Supabase SQL。


## V0.13.12 — 手机农场专用背景

- 桌面版继续使用横向 `images/farm/farm-background.webp`。
- 手机（≤760px）自动切换为 9:16 `images/farm/farm-background-mobile.webp`。
- 手机背景使用独立构图，不再把桌面横图强制裁切。
- 好友拜访农场在手机端也同步使用直式背景。
- 本版仅更换响应式美术与缓存版本号，不需要新增 Supabase SQL。

### V0.13.24 — 无土作物 Atlas / Anchor 测试
- 第一张作物 Sprite Sheet 更换为无土版本，仍保持单一 4×4 大图，不拆成 16 张图片。
- 读取方式沿用 ROWEB 的 Atlas 思路：作物类型决定 row，成长阶段决定 column。
- 新增统一根部 Anchor，并为红萝卜 / 小麦 / 玉米 / 番茄加入独立 scale / lift 微调参数。
- 成熟动画只移动植物视觉，不改变农田命中区；根部尽量保持固定，避免成长阶段切换时跳位。
- 不需要新的 Supabase SQL。


## V0.13.24
- 修正手机端「任务 → 成就」横向分类：左右滑动后点击后方分类时，不再跳回第一个分类的位置。
- 分类内容重绘、领取成就奖励时会保留横向滚动位置。
## V0.13.30 — 每日事件 / 商人 / 虫害
- 每日事件：晴朗、丰收、多雨、雷雨、种子商人来访。
- 商人来访时农舍旁出现 4 帧待机 NPC，并随机提供两种已解锁种子九折。
- 普通作物播种默认 10% 虫害，多雨日 20%；虫害未处理会让该格收成 -1。
- 新增单格除虫与一键除虫；好友拜访可帮忙除虫并有机会获得金币、EXP 或蔬果盲盒。
- 雷雨日普通作物每格收成 -1；晴朗日普通作物成长时间 -5%；丰收日收成 EXP +10%。
- 需在 Supabase 执行 `20260926_015_farm_weather_merchant_pests.sql`。
