const fs=require('fs'),vm=require('vm'),path=require('path');const root=path.resolve(__dirname,'..');
const ctx={window:{},console,Math:Object.create(Math)};ctx.window=ctx;ctx.player={baseLevel:100,stats:{str:80,dex:40,luk:10},equipment:{weapon:1},attackElement:'Fire'};ctx.currentMonster={race:'Brute',size:'Large',element:'Earth',elementLevel:1,def:100,mdef:50,currentHp:10000};ctx.getItemData=()=>({atk:100,weaponLevel:3,weaponType:'sword'});ctx.calculateDerivedPlayerStats=()=>({atk:240,matk:180,stats:ctx.player.stats,cri:0});ctx.getPassiveSkillBonusTotals=()=>({});ctx.getTrainingBonusTotals=()=>({});ctx.getActiveBuffBonusTotals=()=>({});ctx.getPassiveTargetDamageBonus=()=>0;ctx.applyROCombatDamageModifiers=(n,o)=>Math.floor(n*(o.attackElement==='Fire'?1.5:1));vm.createContext(ctx);vm.runInContext(fs.readFileSync(path.join(root,'js/ra_renewal_damage_pipeline.js'),'utf8'),ctx);
function ok(x,m){if(!x)throw Error(m)};let n=ctx.RARenewalDamagePipeline.resolveNormalAttack(ctx.currentMonster,{criticalResult:{critical:false,multiplier:1}});ok(n.damage>0,'normal');let p=ctx.RARenewalDamagePipeline.resolvePhysicalSkill({elementSource:'forced',element:'Neutral',defenseMode:'half'},5,ctx.currentMonster,{ratio:500});ok(p.element==='Neutral'&&p.damage>0,'physical');let m=ctx.RARenewalDamagePipeline.resolveMagicSkill({elementSource:'skill',element:'Fire',matkRatioPerHit:100},10,ctx.currentMonster,{hits:3});ok(m.element==='Fire'&&m.hits===3&&m.damage>0,'magic');console.log(JSON.stringify({PASS:true,normal:n.damage,physical:p.damage,magic:m.damage}));